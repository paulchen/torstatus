full apt updates
don't run as root
hidden services
make updater work (Perl modules from CPAN!)
Docker health checks

document everything in README.md
Tor: keys (/var/lib/tor)

logging (log files, log reporting, syslog)

Nagios status

IP address in X-Forwarded-For?

